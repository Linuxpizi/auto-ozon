# 第一性原理

> 定义不可违背的原则，必须遵守

## 原则列表
1. 不允许定义重复的代码，常量、枚举、函数等等功能相近的必须要抽象，复用
2. 不可猜测、有任何兜底的逻辑，必须要实事求是
3. 数据模型的变更，必须要考虑到迁移问题，而且必须要支持幂等操作
4. 任务期间，如果有过大的窗口，或者文件，必须要进行小任务拆分或者大文件分片处理
5. 不可扩大任务
6. 生成的代码文件拒绝过大，大文件必须拆分模块
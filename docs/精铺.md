# [精铺](https://global-help.ozon.com/zh/products?region=CHN)

这里的精铺有两种方式
1. 通过复制 SKU 精铺，复制 Ozon
2. 其它平台，非 Ozon 店铺

## 主要处理的内容
1. 主图、其它详情图等进行处理(主要是本土化翻译)
2. 文案的翻译

## 相关的 [API](https://docs.ozon.ru/api/seller/zh/?__rr=1#tag/ProductAPI) 接口

## UI

[针对官方的文档说明](https://global-help.ozon.com/zh/products?region=CHN),这里是一系列的官方的文档要求，遵循这里的要求可以流量最大化，以及相关的 [API](https://docs.ozon.ru/api/seller/zh/?__rr=1#tag/ProductAPI)设计精铺模块

## UI 优化
整体的 UI 设计分成左右两部分，左边是原产品信息，右边是优化的产品信息，相关的操作位置有对应的按钮，比如名称，就配有优化商品名称等

1. SKU 复制
商品信息
    1. 名称
    2. 类目和类型
    3. 品牌
    4. 型号名称
    5. 尺寸和重量
       1. 包装长度毫米
       2. 包装宽度毫米
       3. 包装高度毫米
       4. 包装重量克
    6. 变体特征(大的文本输入框，打标签)
    7. 简介
    8. 商品变体
       1. 商品颜色
       2. 货号
       3. 条形码
       4. 价格
       5. 折扣前价格
    以下是折叠的产品信息编辑
    9. 
1. 链接爬取
   1. 商品信息
   2. 特征
   3. 媒体
   4. 预览

## 实现方案

### 1. SKU 复制精铺

#### 流程
1. 输入源 SKU(Ozon 平台商品)
2. 调用 API 获取商品完整信息
3. 翻译处理:
   - 商品名称(俄语 → 目标语言)
   - 商品描述
   - 变体特征
4. 图片处理:
   - 下载原图
   - 可选:添加水印/翻译文字
5. 编辑优化后的信息
6. 创建新商品(草稿状态)
7. 提交审核

#### API 调用链
```
1. POST /v3/product/info/stocks    # 获取库存信息
2. POST /v4/product/info/list      # 获取商品列表详情
3. POST /v1/product/pictures/upload # 上传图片
4. POST /v2/product/import         # 创建商品
```

### 2. 链接爬取精铺

#### 流程
1. 输入商品链接(淘宝/1688/拼多多等)
2. 爬取商品信息:
   - 基本信息(标题、价格、规格)
   - 图片列表
   - SKU 变体
3. AI 翻译处理
4. 图片下载与处理
5. 手动编辑优化
6. 创建 Ozon 商品

### 数据模型

```python
# 精铺任务
class PrecisionListingTask:
    id: int
    store_id: int                    # 目标店铺
    source_type: str                 # "sku_copy" | "link_scrape"
    source_value: str                # SKU 或链接
    status: str                      # "pending" | "processing" | "editing" | "completed"
    
    # 原始数据
    original_data: dict              # 原商品信息
    
    # 处理后数据
    translated_name: str
    translated_description: str
    processed_images: list           # 处理后的图片URL
    
    # 最终数据
    final_data: dict                 # 提交前的完整数据
    ozon_product_id: int             # 创建后的商品ID
    
    created_at: datetime
    updated_at: datetime
```

---

## SKU 详情数据结构（采集商品 ScrapedProduct）

### 数据流总览

```
Ozon 商品页 / 列表页
    ↓ (浏览器插件 DOM 解析)
前端接口 ScrapedProduct (camelCase)
    ↓ POST /browser-sync/sync-products
后端 Schema ScrapedProductCreate → DB 模型 ScrapedProductRecord
    ↓ GET /browser-sync/products
前端读取 ScrapedProductRead → 各视图展示/操作
```

### 1. 后端数据库模型 (`models/scraped_product.py`)

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | Integer (PK) | 自增主键 |
| `platform` | String(16) | `ozon` / `wb` |
| `source_id` | String(64) | 平台商品 ID |
| `title` | String(512) | 商品标题 |
| `price` | Float | 当前价格 (RUB) |
| `old_price` | Float | 原价 |
| `images` | JSON | 图片 URL 列表 `[]` |
| `rating` | Float | 评分 |
| `review_count` | Integer | 评论数 |
| `brand` | String(128) | 品牌 |
| `category` | String(512) | 面包屑类目路径 |
| `discount` | String(32) | 折扣文本 如 `-52%` |
| `stock` | String(128) | 库存文本 |
| `seller_name` | String(256) | 卖家名称 |
| `seller_url` | String(512) | 卖家页面链接 |
| `attributes` | JSON | 属性列表 `[{name, value}]` |
| `description` | Text | 商品描述 |
| `source_url` | String(1024) | 原始商品页面 URL |
| `scraped_at` | DateTime | 插件采集时间 |
| `video_urls` | JSON | 视频 URL 列表 `[]` |
| `sku_list` | JSON | SKU+条形码列表 `[{sku, barcode}]` |
| `spec_list` | JSON | 规格列表 `[{weight_g, depth_mm, height_mm, width_mm, color, size}]` |
| `ozon_category_id` | Integer | Ozon description_category_id |
| `ozon_type_id` | Integer | Ozon type_id |
| `synced` | Boolean | 是否已同步到后端 |
| `matched` | Boolean | 是否已比价匹配 |
| `matched_suppliers` | JSON | 匹配到的供应商信息 |
| `created_at` | DateTime | 记录创建时间 |
| `updated_at` | DateTime | 记录更新时间 |

### 2. 浏览器插件端类型定义 (`browser-extension/src/utils/types.ts`)

```typescript
interface ScrapedProduct {
  platform: 'ozon' | 'wb'
  sourceId: string           // 对应后端 source_id
  title: string
  price: number
  oldPrice: number           // 对应后端 old_price
  images: string[]
  rating: number
  reviewCount: number        // 对应后端 review_count
  brand: string
  category: string
  sellerName: string         // 对应后端 seller_name
  sellerUrl: string          // 对应后端 seller_url
  attributes: ProductAttribute[]
  description: string
  sourceUrl: string          // 对应后端 source_url
  scrapedAt: string          // ISO datetime, 对应后端 scraped_at

  videoUrls: string[]        // 视频 URL 列表
  skuList: Array<{ sku: string; barcode: string }>   // SKU+条形码
  specList: Array<{ weight_g: number; depth_mm: number; height_mm: number; width_mm: number; [key: string]: any }>

  ozonCategoryId: number     // 对应后端 ozon_category_id
  ozonTypeId: number         // 对应后端 ozon_type_id
}

interface ProductAttribute {
  name: string
  value: string
}
```

> **命名转换**：插件端使用 camelCase，后端统一用 snake_case。Schema 的 `model_validator` 自动做 `_camel_to_snake` 转换。

### 3. Ozon 商品详情页采集逻辑 (`ozon.content.ts`)

| 字段 | 采集方式 | DOM 选择器 / 策略 |
|------|---------|-------------------|
| `sourceId` | URL 路径末尾数字 | `/\/(\d+)\/?$/` |
| `title` | 标题元素 | `h1` / `[data-widget="webProductHeading"] span` |
| `price` | 当前价格 | `[data-widget="webPrice"] .tsHeadline600Large` → `parsePrice()` |
| `oldPrice` | 原价（划线价） | `[data-widget="webPrice"] span.pdp_bj` / `text-decoration: line-through` |
| `images` | 商品图库 | `[data-widget="webGallery"] img` → 替换 `/wc\d+/` 为 `/wc500/` 提升清晰度，最多 10 张 |
| `rating` | 评分 | `[data-widget="webReviewProductScore"]` 提取数字 |
| `reviewCount` | 评论数 | 同上，提取整数 |
| `brand` | 品牌 | `[data-widget="webBrandName"] a/span` / `a[href*="/brand/"]` |
| `category` | 面包屑 | `[data-widget="webBreadcrumb"] a` → `slice(1).join(' > ')` |
| `sellerName` | 卖家 | `[data-widget="webMerchantInfo"] a/span` / `[data-widget="webBestSeller"] button span` |
| `attributes` | 多策略属性提取 | 见下文 |
| `description` | 商品描述 | `[data-widget="webDescription"]` → 截取 2000 字符 |
| `sku` | Артикул 编号 | 正则匹配 `Артикул\|SKU` / `meta[itemprop="sku"]` / JSON-LD |
| `variants` | 变体选项 | `[data-widget="webOfferSelector"] button` → 逗号连接 |

**属性提取三策略（优先级递减）：**

1. **展开按钮 + 特征区域**：先点击 `показать все` 按钮展开折叠内容，然后查找 `[data-widget="webCharacteristics"]` 等区域内的名值对 div
2. **通用 div 结构**：遍历容器内 `div > div` 子元素，取前两个子节点作为 name/value
3. **dt/dd 结构**：查找 `dt` 元素及其 `nextElementSibling` 作为名值对

### 4. JSON-LD 补充数据

从 `<script type="application/ld+json">` 中提取 `@type === 'Product'` 的结构化数据：
- `sku` → 补充到 attributes
- `brand.name` → 备用品牌
- `weight` → 重量
- `color` → 颜色

### 5. 后端同步逻辑（Upsert）

```
POST /browser-sync/sync-products
```

- **新记录**：直接插入，`synced=True`
- **已存在**（按 `platform + source_id` 去重）：增量补全更新策略：
  - 标题/卖家/source_url：新值非空则覆盖
  - 品牌/品类/评分/评论数/属性/描述/图片：`_is_enriched()` 判断（新值非空/非零/非空列表）
  - 价格：新值非零则更新
  - discount/stock：新值非空则补全
  - JSON 数组字段（video_urls/sku_list/spec_list）：新值非空时合并更新

### 6. 前端展示

#### 6.1 商品管理页 (`ProductManagementView.vue`)

展示 **Ozon 店铺已上架商品**（来自 listing 模型，非 scraped），数据表列：

| 列 | 字段 | 说明 |
|----|------|------|
| 商品图片 | `primary_image` | 40×40 缩略图 |
| Offer ID | `offer_id` | Ozon 卖家 Offer ID |
| SKU | `sku` | 卖家 SKU |
| 商品名 | `name` | 商品名称 |
| 价格 | `price` / `old_price` | 当前价（有原价时显示划线+当前价） |
| 最低价 | `min_price` | |
| 库存 | `stock_present` / `stock_reserved` | 在库(预留) |
| VAT | `vat` | |
| 货币 | `currency_code` | |
| 状态 | `status` | 在售(绿) / 未在售(黄) / 待售(蓝) |
| KGT | `is_kgt` | 大件标识 |
| 操作 | — | 归档 / 迁移 / 改库存 |

#### 6.2 精铺页 - 采集数据填充 (`PrecisionListingView.vue`)

通过 `POST /precision-listing/scrape` 获取采集数据后，填充到精铺表单：

| 采集字段 | 表单字段 | 处理方式 |
|---------|---------|---------|
| `title` | `source_name` + `translated_name` | 原文直接填入 |
| `description` | `source_description` + `translated_description` | 原文直接填入 |
| `images` | `source_images` | JSON.stringify 序列化 |
| `attributes` | `source_attributes` | 转为 Ozon 标准格式 `[{id, complex_id, name, value, dictionary_value_id}]` |
| `source_id` | `source_sku` | 作为源 SKU 标识 |
| `weight` | `weight` | parseInt 转换 |

#### 6.3 浏览器插件面板 (`ScrapPanel.vue`)

插件弹出面板：
- 检测当前页面类型（商品页 / 列表页 / 不支持）
- 显示平台信息卡片
- 点击"开始采集"触发 `triggerScrape`（商品页）或 `triggerListScrape`（列表页）
- 列表页采集支持进度回调（scraped/enriched/synced/total）
- 采集完成后调用 `POST /browser-sync/sync-products` 同步到后端

## 图片处理策略

| 处理类型 | 说明 | 优先级 |
|---------|------|--------|
| 尺寸裁剪 | 调整为 Ozon 推荐尺寸 | 必需 |
| 背景去除 | AI 自动抠图 | 可选 |
| 水印添加 | 添加店铺 Logo | 可选 |
| 文字翻译 | 图片内文字翻译 | 按需 |

## 翻译配置

```python
translation_config = {
    "source_lang": "zh",           # 源语言
    "target_lang": "ru",           # 目标语言(俄罗斯站)
    "ai_model": "gpt-4",          # 翻译模型
    "seo_optimize": True,          # SEO 优化
    "keyword_research": True,      # 关键词研究
}
```

## 注意事项

1. **图片要求**
   - 最少 5 张图片
   - 主图白底
   - 分辨率不低于 200x200
   - 文件大小不超过 5MB

2. **文案要求**
   - 标题长度 15-150 字符
   - 避免敏感词
   - 包含核心关键词

3. **价格策略**
   - 考虑平台佣金
   - 预留促销空间
   - 参考竞品定价
# 复刻实现

> 复刻项目 **ozon-plugin-3.1.0**，优化现有项目 **extension**
>

## 执行步骤
1. 复刻 UI 实现
   1. 什么 logo 名称类似的，依旧使用现有项目
2. 整理、验证指标数据采集是否依赖后端第三方服务，如果不依赖，复刻其实现，替换现在的指标数据采集
   1. 需要 seller.ozon.ru 的 cookie
      1. 验证
      2. 绑定
   2. 数据指标采集完全复刻就行，不用关心现有实现
   3. 必须依次验证，不允许一直无休止的修改，必须阶段性 MVP 验证
   4. 验证使用打开浏览器的方式，直接干净的验证修改结果

## 启动爬虫

> UI 设计
> 把设置选品，集成到启动爬虫，增加目标采集数目，是否采集 SKU 变体。
> 默认采集数目100，只采集当前 SKU，不采集 SKU 变体
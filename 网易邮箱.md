# 163网易邮箱开启“发送邮箱验证”完整流程

## 场景一：用163邮箱给用户发验证码（开发者用）

### 1. 开启 SMTP 服务
- 登录网页版 163 邮箱（mail.163.com）
- 点击右上角「设置」→「POP3/SMTP/IMAP」
- 找到「IMAP/SMTP 服务」，点击「开启」

### 2. 获取授权码（最关键）
- 用绑定的手机号按提示发送短信
- 点击「我已发送」，系统会生成一串 **授权码**（16位字母+数字）
- **立即复制保存**，关闭页面后不再完整显示

### 3. 代码配置参数
| 参数        | 值                          |
| ----------- | --------------------------- |
| SMTP 服务器 | smtp.163.com                |
| 端口（SSL） | 465                         |
| 端口（TLS） | 587                         |
| 账号        | 你的完整邮箱（xxx@163.com） |
| 密码        | **授权码**（不是登录密码）  |

### Python 发送示例
```python
import smtplib
from email.mime.text import MIMEText

smtp_server = "smtp.163.com"
port = 465
sender = "你的邮箱@163.com"
password = "你的授权码"

msg = MIMEText("您的验证码是：123456", "plain", "utf-8")
msg["Subject"] = "验证码"
msg["From"] = sender
msg["To"] = "接收邮箱@qq.com"

with smtplib.SMTP_SSL(smtp_server, port) as server:
    server.login(sender, password)
    server.sendmail(sender, [msg["To"]], msg.as_string())
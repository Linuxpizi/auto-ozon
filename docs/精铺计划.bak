# 精铺计划

## 概述

精铺（Precision Listing）是指通过复制其他卖家的 Ozon 商品卡片，进行内容本地化（翻译、图片处理）和定价优化后，在自己的店铺中上架销售的运营方式。

### 两种精铺模式

| 模式 | 说明 | 核心 API |
|------|------|----------|
| **模式A：复制 Ozon 商品** | 输入 SKU，复制其他卖家的商品卡片 | `POST /v1/product/import-by-sku` |
| **模式B：外部平台导入** | 从 1688/速卖通等平台手动录入商品信息 | `POST /v2/product/import` |

---

## 工作流程

### 阶段一：商品获取

**模式A - 从 Ozon 复制：**
1. 输入源商品 SKU（多个 SKU 批量操作）
2. 通过 `POST /v3/product/info/list` 获取商品基本信息
   - 名称、描述、图片列表、barcodes
   - 商品类目（category_id、type_id）
   - 库存、价格信息
3. 通过 `POST /v4/product/info/attributes` 获取商品属性详情
   - 所有属性值（品牌、颜色、尺寸等）
   - 属性字典映射
4. 下载主图和详情图到本地缓存

**模式B - 外部平台导入：**
1. 手动填写商品信息（标题、描述、图片 URL）
2. 选择目标 Ozon 类目
3. 手动映射属性

### 阶段二：内容本地化

1. **标题翻译** - 中文→俄文，遵循 Ozon 标题规范
   - 包含品牌、型号、核心卖点
   - 长度 15-150 字符
   - 避免特殊字符和全大写
2. **描述翻译** - HTML 格式保留，中文→俄文
   - 结构化描述：要点列表 + 详细说明
   - 包含规格参数
3. **属性值翻译** - 对可翻译属性进行本地化
   - 品牌名保持不变
   - 颜色、材质等属性值翻译
4. **图片处理**（可选）
   - 添加俄文水印
   - 图片尺寸合规检查（推荐 900×1200）

### 阶段三：定价与配置

1. **设置售价**（₽）- 参考同类商品定价
2. **设置划线价** - 用于显示折扣
3. **VAT 税率** - 通常 0%（跨境卖家）
4. **选择目标店铺** - 从已绑定的店铺列表选择

### 阶段四：发布

1. **预览商品卡片** - 确认翻译和图片效果
2. **创建精铺任务** - 记录操作历史
3. **提交到 Ozon** - 调用 API 创建商品
4. **状态追踪** - 审核状态、上架状态

---

## 关键 API 接口

| API 端点 | 方法 | 用途 |
|----------|------|------|
| `/v3/product/info/list` | POST | 根据标识符获取商品信息 |
| `/v4/product/info/attributes` | POST | 获取商品属性详情 |
| `/v1/product/import-by-sku` | POST | 通过 SKU 复制商品（精铺核心） |
| `/v2/product/import` | POST | 创建/更新商品（外部平台导入） |
| `/v1/product/info/description` | POST | 获取商品描述 |
| `/v4/product/info/stocks` | POST | 查询库存 |
| `/v1/product/info` | POST | 获取单个商品详情 |
| `/v1/product/info/archives` | POST | 归档商品 |

---

## 数据模型

### 精铺任务 (PrecisionListingTask)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 主键 |
| store_id | Integer | 目标店铺 ID |
| store_name | String | 店铺名称 |
| mode | String | 精铺模式: `copy_ozon` / `external` |
| source_sku | String | 源商品 SKU（模式A） |
| source_name | String | 源商品名称 |
| source_images | Text | 源商品图片 JSON |
| source_description | Text | 源商品描述 |
| source_attributes | Text | 源商品属性 JSON |
| category_id | Integer | Ozon 类目 ID |
| type_id | Integer | Ozon 商品类型 ID |
| translated_name | String | 翻译后标题 |
| translated_description | Text | 翻译后描述（HTML） |
| translated_attributes | Text | 翻译后属性 JSON |
| price | String | 售价（₽） |
| old_price | String | 划线价（₽） |
| vat | String | VAT 税率 |
| offer_id | String | 新商品 Offer ID（创建后回填） |
| product_id | String | Ozon Product ID（创建后回填） |
| status | String | 状态: draft / translating / submitted / active / rejected |
| error_message | Text | 错误信息 |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

---

## UI 设计

### 页面结构

```
精铺管理
├── 顶部操作栏
│   ├── 新建精铺任务按钮
│   └── 筛选器（店铺、状态、模式）
├── 任务列表
│   ├── 任务卡片/表格
│   ├── 状态标签
│   └── 操作按钮（编辑/提交/删除）
└── 新建/编辑任务（弹窗或子页面）
    ├── Step 1: 选择模式 & 输入来源
    ├── Step 2: 预览 & 编辑内容
    ├── Step 3: 定价 & 配置
    └── Step 4: 确认 & 提交
```

### UI 交互流程

1. **任务列表页**：展示所有精铺任务，支持按店铺/状态筛选
2. **新建任务**：
   - 选择模式（复制Ozon / 外部导入）
   - 输入 SKU 或手动填写信息
   - 点击"获取"拉取源商品数据
3. **编辑任务**：
   - 左侧：原文（俄文），右侧：翻译（中文）
   - 图片预览和管理
   - 属性编辑表格
4. **提交任务**：
   - 确认所有信息
   - 选择目标店铺
   - 点击提交
   - 跳转到列表查看状态

---

## 开发计划

### Phase 1: 基础框架
- [ ] 数据模型和数据库迁移
- [ ] 基础 CRUD API
- [ ] 列表页 UI

### Phase 2: 商品获取
- [ ] Ozon API 集成（获取商品信息和属性）
- [ ] SKU 输入和验证
- [ ] 商品预览

### Phase 3: 内容编辑
- [ ] 标题编辑器
- [ ] 描述编辑器（富文本）
- [ ] 属性编辑表格
- [ ] 图片管理

### Phase 4: 发布
- [ ] 定价配置
- [ ] 提交到 Ozon
- [ ] 状态追踪
